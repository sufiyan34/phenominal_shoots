"use client";
import Link from "next/link";import{useEffect,useState}from"react";import{listPublished}from"@/lib/publicData";import type{Promotion}from"@/lib/types";
export default function PromotionsPage(){const[items,setItems]=useState<Promotion[]>([]);useEffect(()=>{void listPublished<Promotion>("promotions").then(setItems).catch(()=>{})},[]);return <main style={{paddingTop:140}}><section className="section"><div className="shell"><div className="eyebrow">Offers</div><h1 className="display" style={{fontSize:"clamp(60px,9vw,120px)",lineHeight:.85,margin:"18px 0 45px"}}>Current promotions.</h1>{!items.length?<p style={{color:"var(--muted)"}}>No active promotions right now.</p>:<div style={{display:"grid",gridTemplateColumns:"repeat(2,1fr)",gap:20}}>{items.map((p)=><Link key={p.id} href={`/promotions/${p.slug}`}><div className="media" style={{height:360}}>{p.image&&<img src={p.image} alt={p.title}/>}</div><div style={{paddingTop:14}}><div className="eyebrow">{p.offer}</div><h2 className="display" style={{fontSize:35,margin:"8px 0"}}>{p.title}</h2><p style={{color:"var(--muted)",lineHeight:1.7}}>{p.description}</p></div></Link>)}</div>}</div></section></main>}
