"use client";
import{useEffect,useState}from"react";import Link from"next/link";import{doc,getDoc}from"firebase/firestore";import{db}from"@/lib/firebase/client";
export default function AboutPage(){const[about,setAbout]=useState("We don't just capture the event. We capture how it felt.");useEffect(()=>{getDoc(doc(db,"site_settings","main")).then(s=>{if(s.exists()&&typeof s.data().about==='string')setAbout(s.data().about)})},[]);return <main style={{paddingTop:140}}><section className="section"><div className="shell grid-12"><div style={{gridColumn:"span 5"}}><div className="eyebrow">The studio</div><h1 className="display" style={{fontSize:"clamp(58px,8vw,110px)",lineHeight:.85}}>About the photographer.</h1></div><div style={{gridColumn:"span 7",fontSize:22,lineHeight:1.7,color:"var(--muted)"}}><p>{about}</p><p>Phenomenal Shoots is a visual studio built around honest emotion, considered composition and cinematic storytelling across photography and motion.</p><Link className="btn btn-dark" href="/contact">Get in touch</Link></div></div></section></main>}
