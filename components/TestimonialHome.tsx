"use client";import{useEffect,useState}from"react";import{listPublished}from"@/lib/publicData";import{Testimonial}from"@/lib/types";
export default function TestimonialHome(){const[t,setT]=useState<Testimonial|null>(null);useEffect(()=>{void listPublished<Testimonial>("testimonials",1).then(x=>setT(x[0]??null)).catch(()=>{})},[]);if(!t)return null;return <section className="section"><div className="shell" style={{maxWidth:900,textAlign:"center"}}><div className="eyebrow">Client words</div><blockquote className="display" style={{fontSize:"clamp(34px,5vw,64px)",lineHeight:1.1,margin:"22px 0"}}>“{t.quote}”</blockquote><div className="eyebrow">{t.clientName}{t.role?` · ${t.role}`:""}</div></div></section>}
