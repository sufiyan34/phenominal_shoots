"use client";import{useEffect,useState}from"react";import{listPublished}from"@/lib/publicData";import type{Testimonial}from"@/lib/types";export default function TestimonialPublicList(){const[items,setItems]=useState<Testimonial[]>([]);useEffect(()=>{void listPublished<Testimonial>("testimonials").then(setItems).catch(()=>{})},[]);return <div style={{display:"grid",gap:18,maxWidth:850,margin:"0 auto"}}>{items.map(t=><blockquote key={t.id} style={{margin:0,padding:"25px 0",borderBottom:"1px solid var(--line)"}}><div className="display" style={{fontSize:28,lineHeight:1.35}}>“{t.quote}”</div><footer style={{marginTop:16,fontSize:12,color:"var(--muted)",textTransform:"uppercase",letterSpacing:".1em"}}>{t.clientName}{t.role?` · ${t.role}`:""}</footer></blockquote>)}</div>}
